# Specify desired highlighting theme (e.g. "TwoDark"). Run `bat --list-themes` for a list of all available themes
--theme="Dracula"

# Enable this to use italic text on the terminal. This is not supported on all terminal emulators (like tmux, by default):
--italic-text=always

# Don't show line numbers
--style=plain
